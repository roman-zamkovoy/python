Условие:

Требуется написать программу которая принимает на вход таблицу 4 на 4, требуется вывести на экран сумму всех элементов матрицы которые находятся выше главной диагонали. 

Пример:

Ввод:
a = [[1,2,3,2],[1,3,2,2],[1,0,2,1],[1,3,6,9]]

Вывод:
12


Подсказка по коду:
a = [[...], [...], [...], [...]]

sum = 0
for i in range(len(a)):
  for j in range(len(a)):
    if(...):
      sum ...
...

Подсказка: элемент находится выше главной диагонали ЕСЛИ i < j
